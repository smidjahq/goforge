services:
  app:
    build: .
    ports:
      - "${HTTP_PORT:-8080}:8080"
    environment:
      HTTP_PORT: "8080"
    env_file:
      - .env
{{- if ne .DB "none"}}
    depends_on:
      db:
        condition: service_healthy
{{- end}}
