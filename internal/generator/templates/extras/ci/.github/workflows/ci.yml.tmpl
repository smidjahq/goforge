name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "{{if .GoVersion}}{{.GoVersion}}{{else}}1.23{{end}}"

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./... -race -count=1
